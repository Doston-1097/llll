import React from 'react';
import DataTable from "../../components/table/table";
import {Typography} from "@mui/material";

function Reyting(props) {
    return (
        <>
            <Typography variant="h1" component={"h1"}>Reyting</Typography>
            {/*<DataTable rows={props.rows} columns={props.columns} actions={props.actions}/>*/}
        </>
    );
}

export default Reyting;